# Methond - Bruteforce
#```python
# def rev(nums, k):

#     for i in range(0, k):
#         e = nums.pop()
#         nums.insert(0,e)
#     return nums
    
    
# print(rev([1, 2, 3, 4, 5], 9))
#```


# ```python
# def rev(nums, k):

#     for i in range(0, k):
#         nums[:] = nums[-1:] + nums[:-1]
#     return nums
    
# print(rev([1, 2, 3, 4, 5], 9))
# ```

### Time Complexity: O(N)
### Space Complexity: O(1)

#```python
# def rev(nums, k):
#     rotations = k % 10

#     for i in range(0, rotations):
#         e = nums.pop()
#         nums.insert(0,e)
#     return nums
    
    
# print(rev([1, 2, 3, 4, 5], 9))
#```
### Time Complexity: O(rotations*N)
### Space Complexity: O(1)

# MEthod - Better Solution
# ```python
# def rev(nums, k):
#     r = k % 10
#     n = len(nums)
#     nums[:] = nums[n-r:] + nums[:n-r]
#     return nums
    
# print(rev([1, 2, 3, 4, 5], 2))
# ```
### Time Complexity: O(N)
### Space Complexity: O(1)

# Method Optimal
# ```python
# def reverse(nums, left, right):
#     while left < right:
#         nums[left], nums[right] = nums[right], nums[left]
#         left += 1 
#         right -= 1 

# def rev(nums, k):
#     r = k % 10
#     n = len(nums)
#     reverse(nums, n-r, n-1)
#     reverse(nums, 0, n-r-1)
#     reverse(nums, 0, n-1)
#     return nums
    
# print(rev([1, 2, 3, 4, 5], 2))
# ```
### Time Complexity: O(N)
### Space Complexity: O(1)


